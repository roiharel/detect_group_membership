# Split Analysis Utilities\n\n# Load necessary libraries\nlibrary(ggplot2)\nlibrary(dplyr)\nlibrary(viridis)\n\n# Function to create density plot of n_clusters per group\ndensity_plot_n_clusters <- function(data, n_clusters_col, group_col) {\n  ggplot(data, aes_string(x = n_clusters_col, group = group_col)) +\n    geom_density(aes(fill = as.factor(.data[[group_col]])), alpha = 0.5) +\n    geom_smooth(se = FALSE) +\n    scale_fill_viridis(discrete = TRUE) +\n    labs(title = "Density Plot of N Clusters per Group") +\n    theme_minimal()\n}  \n\n# Function to analyze collars vs splits\ncollars_vs_splits_analysis <- function(flags, groups, n_individuals_col) {\n  # Dynamic analysis implementation here\n  # Use flags to determine individuals involved in the analysis\n  # Return a summary or visualization\n}  \n\n# Function for month effect analyses\nmonth_effect_analysis <- function(data, group_col) {\n  # Analysis implementation here, aggregated by month and group\n  # Return summary statistics or visualizations\n} \n\n# Function to count the number of individuals in split clusters\ncount_individuals_in_split_clusters <- function(splits_named) {\n  # Count logic implementation here\n  # Ensure robust handling of NA values\n} \n\n# Main function to run splits analysis plots\nrun_splits_analysis_plots <- function(flags, splits_named, group_col) {\n  # Ensure NA groups are handled\n  # Call the relevant analysis functions and plot results\n}  \n\n# Export functions\nexported_functions <- list(\n  density_plot_n_clusters,\n  collars_vs_splits_analysis,\n  month_effect_analysis,\n  count_individuals_in_split_clusters,\n  run_splits_analysis_plots\n)\n